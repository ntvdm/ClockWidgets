<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Musti" parent="@android:style/Theme.Holo">
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:actionBarStyle">@style/Widget.ActionBar.Flat</item>
    </style>
    <style name="Widget.ActionBar.Flat" parent="@android:style/Widget.Holo.ActionBar.Solid">
        <item name="android:displayOptions">showHome|showTitle|useLogo</item>
    </style>

</resources>
